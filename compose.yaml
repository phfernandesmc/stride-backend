services:
  postgres:
    image: "postgres:18"
    environment:
      - "POSTGRES_DB=stride_db"
      - "POSTGRES_PASSWORD=senhaTest123"
      - "POSTGRES_USER=postgres"
    ports:
      - "5432:5432"
